box: debian
build:
  steps:
    - arjen/hugo-build@1.8.1:
        version: "0.15"
deploy:
  steps:
    - install-packages:
        packages: git ssh-client
    - leipert/git-push@0.7.6:
         gh_oauth: $GH_TOKEN
         gh_pages: true
         basedir: build